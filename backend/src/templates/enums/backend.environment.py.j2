"""Environment enumeration for {{ project_name }}."""

from enum import StrEnum, auto


class Environment(StrEnum):
    """Application environment types."""

    DEVELOPMENT = auto()
    STAGING = auto()
    PRODUCTION = auto()
